<% @songs.each do |song| %>
    <p> <%= @song.title %> </p>
    <p> <%= link_to(song.title, song_path(song)) %> </p>
    <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
    <% end %>