<tbody>
<% @songs.each do |song| %>
   <tr> <%= link_to(song.title, song_path(song)) %> </tr>
    <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
    <br>
    <% end %>
</tbody>